<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">  
<properties>  
	<comment>IK Analyzer 扩展配置</comment>
	<!--用户可以在这里配置自己的扩展字典 
	<entry key="ext_dict">ext.dic;</entry> 
	-->
	<!--用户可以在这里配置自己的扩展停止词字典-->
	<entry key="ext_stopwords">stopword.dic;chinese_stopword.dic;english_stopword.dic</entry> 
        <entry key="ext_dict">jieba.dic;ext.dic;ext_databases.dic;hotel.name.dic;place.name.dic;hotel_code_area.dic;hotel_code_brand.dic;hotel_code_city.dic;hotel_code_ref.dic;hotel_code_seg.dic;hotel_code_thc.dic</entry>	
</properties>
